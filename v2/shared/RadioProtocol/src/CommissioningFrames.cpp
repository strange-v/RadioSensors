#include "CommissioningFrames.h"

// The codec is header-only so it remains usable by small node targets without
// link-order requirements. This translation unit keeps every gateway build as
// an additional compile check for the complete commissioning wire format.
